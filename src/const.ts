export enum AppRoute {
  Root = '/',
  Login = '/login',
  Favorites = '/favorites',
  Offer = '/offer',
  Page404 = '*',
  DevRoot = '/dev-root',
  DevFavorites = '/dev-favorites',
  DevOffer = '/dev-offer',
}

export enum AuthStatus {
  Auth = 'AUTH',
  NoAuth = 'NO_AUTH',
  Unknown = 'UNKNOWN',
}
